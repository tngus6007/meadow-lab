export const species=[{id:'grass',name:'Meadow grass',role:'Producer',food:[],water:3,light:4},{id:'clover',name:'White clover',role:'Producer',food:[],water:4,light:3},{id:'rabbit',name:'Rabbit',role:'Herbivore',food:['grass','clover']},{id:'vole',name:'Field vole',role:'Herbivore',food:['grass']},{id:'fox',name:'Red fox',role:'Predator',food:['rabbit','vole']}];
export const scenarios={healthy:{name:'Sunny meadow',water:6,light:6,levels:[6,6,6,6,6]},dry:{name:'Dry spell',water:1,light:6,levels:[6,6,6,6,6]},shade:{name:'Deep shade',water:6,light:1,levels:[6,6,6,6,6]},repair:{name:'The missing grass',water:6,light:6,levels:[0,6,5,3,4],absent:['grass']},final:{name:'The covered patch',water:2,light:2,levels:[2,2,3,2,2]}};
export function create(key='healthy'){const s=scenarios[key];return {water:s.water,light:s.light,tick:0,organisms:Object.fromEntries(species.map((x,i)=>[x.id,{present:!s.absent?.includes(x.id),level:s.levels[i]}]))};}
export const clamp=n=>Math.max(0,Math.min(10,n));
export function step(state){const next=structuredClone(state),log=[];next.tick++;for(const s of species){const old=state.organisms[s.id];if(!old.present){next.organisms[s.id].level=0;log.push({name:s.name,before:0,after:0,reason:'Not included in this habitat.'});continue;}const enough=s.role==='Producer'?state.water>=s.water&&state.light>=s.light:s.food.some(id=>state.organisms[id].present&&state.organisms[id].level>=4);const delta=enough?1:-2;next.organisms[s.id].level=clamp(old.level+delta);const reason=s.role==='Producer'?(enough?'Enough light and water to support growth.':`Needs water at least ${s.water} and light at least ${s.light}; a resource is too low.`):(enough?'A food source had support of at least 4 at the start of this step.':`Needs ${s.food.map(id=>species.find(x=>x.id===id).name).join(' or ')} with support at least 4. Food support is too low.`);log.push({name:s.name,before:old.level,after:next.organisms[s.id].level,reason});}return {state:next,log};}
export function toggle(state,id){const next=structuredClone(state),o=next.organisms[id];o.present=!o.present;o.level=o.present?4:0;return next;}
export function viable(state){return species.every(s=>state.organisms[s.id].present&&state.organisms[s.id].level>=4)&&(state.water>=4&&state.light>=4);}
export const plantTotal=state=>state.organisms.grass.level+state.organisms.clover.level;
